/** @type {import('next').NextConfig} */
const nextConfig = {
  // Mode server standar (bukan static export) untuk mendukung API routes,
  // Server Actions, dan Middleware sesuai arsitektur TEMANIN
};

export default nextConfig;
